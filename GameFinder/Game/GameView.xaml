<UserControl
    x:Class="GameFinder.Game.GameView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converter="clr-namespace:GameFinder.Converter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    HorizontalAlignment="Center"
    d:DesignHeight="150"
    d:DesignWidth="300"
    Background="{DynamicResource MaterialDesignPaper}"
    FontFamily="{DynamicResource MaterialDesignFont}"
    TextElement.FontSize="13"
    TextElement.FontWeight="Regular"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    TextOptions.TextFormattingMode="Ideal"
    TextOptions.TextRenderingMode="Auto"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converter:UrlToImageConverter x:Key="UrlToImageConverter" />
    </UserControl.Resources>

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Image
            Grid.Row="0"
            Grid.Column="0"
            Margin="15"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Source="{Binding IconUrl, Converter={StaticResource UrlToImageConverter}}" />

        <TextBlock
            Grid.Row="0"
            Grid.Column="1"
            Margin="15"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            FontSize="20"
            Text="{Binding Name, FallbackValue=Unknown}"
            TextTrimming="CharacterEllipsis"
            TextWrapping="Wrap" />

        <TextBlock
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Margin="5"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            FontSize="13"
            Foreground="LightGray"
            Text="{Binding Playtime, FallbackValue=Playtime: 0:00, StringFormat=Playtime: {0}}"
            TextTrimming="CharacterEllipsis"
            TextWrapping="Wrap" />
    </Grid>
</UserControl>
